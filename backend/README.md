# Social Media Auditor

Basic instructions to run the backend server for development and test.

Run the server (development):

```bash
npm run dev
```

Start only the server (no frontend build):

```bash
npm run start:server
```

Run tests:

```bash
npm test
```

Health endpoint:

- GET /health — returns service status JSON

